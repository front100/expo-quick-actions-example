import { Stack } from 'expo-router';

const SettingsLayout = () => {
  return <Stack screenOptions={{ headerTitle: 'Settings', headerTitleAlign: 'center' }} />;
};
export default SettingsLayout;
